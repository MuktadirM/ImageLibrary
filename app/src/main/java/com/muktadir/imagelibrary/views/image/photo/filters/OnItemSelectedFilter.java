package com.muktadir.imagelibrary.views.image.photo.filters;

import com.muktadir.photoeditor.utils.PhotoFilter;

public interface OnItemSelectedFilter {
    void onFilterSelected(PhotoFilter photoFilter);
}
